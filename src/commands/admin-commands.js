// admin-commands.js

// Comandos administrativos

// Comando para expulsar um usuário
function kick(user) {
  // Lógica para expulsar o usuário
}

// Comando para promover um usuário
function promote(user) {
  // Lógica para promover o usuário
}

// Comando para rebaixar um usuário
function demote(user) {
  // Lógica para rebaixar o usuário
}

// Comando para silenciar um usuário
function mute(user) {
  // Lógica para silenciar o usuário
}

// Comando para retirar o silêncio de um usuário
function unmute(user) {
  // Lógica para retirar o silêncio do usuário
}
